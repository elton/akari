{
  "name": "akari-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "akari core — Qwen Realtime client, provider abstraction, tool registry, memory. Talks to akari.app over a Unix domain socket.",
  "module": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  },
  "dependencies": {}
}
